# Meson cross file: build picolibc with the NanOS x86_64-elf cross-gcc.
# Mirror of picolibc-i686-elf.txt for the long-mode (x86_64) sysroot. The toolchain is
# already on PATH (/opt/cross/bin in the nanos-build image).
[binaries]
c = 'x86_64-elf-gcc'
ar = 'x86_64-elf-ar'
as = 'x86_64-elf-as'
strip = 'x86_64-elf-strip'

[host_machine]
system = 'none'
cpu_family = 'x86_64'
cpu = 'x86_64'
endian = 'little'

[properties]
# The cross-gcc cannot link a hosted test program (freestanding), so skip Meson's
# compiler sanity link check.
skip_sanity_check = true
